FROM node:22-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    golang-go \
    git \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -m -s /bin/bash kiro-user
USER kiro-user
WORKDIR /workspace

